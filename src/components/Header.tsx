import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1 className="glitch">CodeCraft Innovations</h1>
        </header>
    );
};

export default Header;
